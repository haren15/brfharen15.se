<div class="center">
  <h2><%= news_item.title %></h2>
  <p><%= truncate news_item.description, length: 200 %></p>
  <p>
    <%= link_to 'Läs mer', news_item, class: 'btn btn-primary' %>
    <% if admin_signed_in? %>
      <a href="/admin/news_item/<%= news_item.id %>/edit" class="btn btn-primary">
        <i class="fa fa-edit"></i>
      </a>
    <% end %>
  </p>
</div>
